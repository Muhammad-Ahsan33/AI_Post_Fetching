name: Keep Render Awake

on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -I https://ai-post-fetching.onrender.com   # or /health if you have one
